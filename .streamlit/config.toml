# Shared theme so the dashboard looks the same on every machine.
[theme]
base = "light"
primaryColor = "#1f5a8c"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f5f7f9"
textColor = "#22303c"
font = "sans serif"

[server]
headless = false

[browser]
gatherUsageStats = false
